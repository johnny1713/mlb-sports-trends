# MLB 賽事趨勢爬蟲與互動式儀表板

本專案是一個高效、輕量且零外部依賴的 Python 腳本。它能自動抓取 covers.com 每日的 MLB 對戰組合與趨勢（Trends）數據，並根據特定的投注邏輯進行智能媒合，最後生成一個極具視覺質感的本機互動式網頁儀表板（`mlb_trends.html`），協助您輕鬆分析並篩選出高勝率的投注組合。

## 需要用戶確認的重點

> [!IMPORTANT]
> **數據抓取策略與穩定性**
> - 本腳本使用 Python 標準庫 `urllib.request` 並搭配自訂瀏覽器標頭（User-Agent）直接獲取 covers.com 的網頁內容。經測試，此方式能穩定且快速地抓取數據，無需安裝 Selenium 或 Playwright 等大型瀏覽器驅動，可實現零依賴、極速運行。
> - 趨勢數據將使用高強度的正則表達式（regex）精準解析網頁中的 `<h4 class="High">` 與 `<h4 class="Low">` 標籤以及 JSON-LD 結構。

> [!TIP]
> **本機互動式儀表板 (`mlb_trends.html`)**
> - 執行腳本後，將在專案目錄下生成一個完全獨立的 `mlb_trends.html` 網頁檔案。
> - 網頁將採用高端的深色主題與玻璃擬物化（Glassmorphism）卡片佈局，並搭配亮眼色彩標記（高亮綠色代表高獲利、警示紅色代表虧損趨勢），以及平滑的微動畫與自適應篩選器。
> - 您只需在瀏覽器中直接按兩下打開此 HTML 檔案即可使用，無需運行本機網頁伺服器。

---

## 待確認問題

> [!NOTE]
> 目前沒有懸而未決的問題。媒合邏輯與需求非常明確：
> 1. **雙向正面趨勢（雙正面）**：對戰的兩支球隊在同一個大小分盤口（全場大小、主客隊大小等）都呈現獲利的正面趨勢（例如：兩隊皆偏向開出 Under 小分）。
> 2. **一正一反趨勢**：在獨贏盤（Moneyline）或讓分盤（Run Line）中，A 隊呈現高獲利正面趨勢（High / 正 Units），而對手 B 隊呈現高虧損負面趨勢（Low / 負 Units）。這代表投注 A 隊有極高的獲利機會。

---

## 預計變更

### [MLB 趨勢核心組件]

我們將在您的工作目錄 `C:\Users\johnn\OneDrive\桌面\Projects\sports` 中建立全新的 Python 腳本 `scrape.py`。

#### [NEW] [scrape.py](file:///C:/Users/johnn/OneDrive/桌面/Projects/sports/scrape.py)
該腳本將執行以下核心操作：
1. **抓取每日對戰**：獲取每日賽事列表頁面（例如：`https://www.covers.com/sports/mlb/matchups`，支援透過 `?date=YYYY-MM-DD` 參數查詢特定日期）。解析出每場比賽的 picks 頁面路徑（`/sport/baseball/mlb/matchup/\d+`）。
2. **抓取詳細趨勢**：針對每場比賽，獲取 `https://www.covers.com/sport/baseball/mlb/matchup/<ID>/picks`。解析出：
   - 對戰隊伍名稱（例如："Los Angeles Angels vs Detroit Tigers"）
   - 包含在 `<h4 class="High">` 和 `<h4 class="Low">` 中的所有正面與負面趨勢文字。
3. **解析與標準化趨勢**：
   - 提取 Units 獲利值（如 `+6.05 Units`）與投資報酬率 ROI（如 `22% ROI`）。
   - 將趨勢歸類為特定的盤口市場（獨贏盤 Moneyline、讓分盤 Run Line、首五局獨贏、首五局讓分、全場大小、隊伍大小等）以及趨勢方向（Under 小、Over 大、贏盤、輸盤等）。
4. **智能媒合運算**：
   - **雙正面（Double Positive）**：篩選出雙方皆看好 Under 或皆看好 Over 的大小分組合。
   - **一正一反（Opposing Trends）**：在獨贏或讓分盤中，找出一方極強（正面）、另一方極弱（負面）的黃金投注標的。
5. **生成儀表板**：自動將抓取與分析後的結果寫入結構精美、中文化的 `mlb_trends.html`。

---

## HTML 互動儀表板視覺設計

生成的 `mlb_trends.html` 將具備極致尊榮的現代網頁設計質感：
- **色彩系統**：以深邃藍黑（`#0b0f19`）為底色，搭配半透明毛玻璃質感卡片、亮綠色（`#00E676`）代表高回報正面趨勢、警示紅（`#FF5252`）代表負面趨勢。
- **盤口標籤**：使用精緻的霓虹圓角徽章（Badges）區分盤口種類（全場大小、獨贏盤、讓分盤等）。
- **互動體驗**：
  - 設計頂部頁籤，可一鍵切換**「全部對戰組合」**、**「🔥 雙向正面推薦 (大小分)」**與**「🎯 一正一反推薦 (獨贏/讓分)」**。
  - 每場比賽卡片均可點擊展開，查閱該場比賽被爬取的所有原始趨勢數據。
  - 提供即時搜尋框，輸入隊伍名稱即可快速過濾賽事。
- **排版細節**：採用現代無襯線 Inter 字型、細緻的 hover 懸停縮放與發光特效、響應式格狀佈局（RWD）。

---

## 驗證計劃

### 自動測試
- 於終端機執行 `python scrape.py`，確認腳本順利執行完畢且無報錯。
- 檢查專案目錄下是否成功生成包含完整數據的 `mlb_trends.html`。

### 手動驗證
- 雙擊開啟生成的 `mlb_trends.html`，測試所有網頁功能（分類切換、搜尋、點擊展開詳細趨勢）。
- 對照 covers.com 當日數據，確保媒合演算法的篩選結果 100% 精準。
