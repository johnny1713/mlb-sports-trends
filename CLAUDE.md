# MLB Trends 專案筆記

MLB 每日賽事趨勢爬蟲 + GitHub Pages 儀表板。抓 covers.com 的賽前趨勢，
以「過盤命中率」篩選推薦，GitHub Actions 每日自動更新。

## 使用情境（設計依據）

- 使用者在台灣，每天**約 22:00 (台灣時間)** 開手機看網站，依推薦在 playsport.cc 下注。
- 使用者**只在意會不會過盤（命中率）**，不看 ROI/Units——covers 的 ROI 是用美國賠率算的，
  對台灣運彩玩家沒有意義。所有 ROI 顯示已於 2026-07-10 全面移除。
  ⚠️ 但 2026-07-10 那次**只清了推薦卡片**，趨勢原文 `(+6.65 Units / 47% ROI)` 一直還在，
  經 `processed_trends` 顯示在詳細趨勢區，也被複製進 `strong_trend`／`weak_trend`／
  `team_x_trends`（實測一天 109＋44 處）。2026-08-05 改在解析源頭用 `strip_roi_text()` 砍掉，
  **新增任何顯示趨勢原文的地方都會自動乾淨**。`fmt_hit()` 無法解析場次時也不再退回 Units。
- 手機瀏覽為主，UI 改動優先考慮窄螢幕。

## covers.com 的重要行為（實測，2026-07-10）

這些是踩過坑才知道的，改爬蟲前務必先讀：

1. **預設 matchups 頁在美東清晨仍顯示「前一天已打完」的比賽**，換日時間約在美東 8 點前後。
   必須明確帶 `?selectedDate=<美東今日>` 參數（舊參數 `?date=` 已被改名棄用、會被靜默忽略）。
   這個 bug 曾讓系統長期分析已結束的賽事而無人發現——「推薦每天都滿載」反而是症狀。
2. **趨勢是賽前漸進發佈的**：美東凌晨 5 點幾乎全空，6 點約 1/4 場次有，**8 點半左右大致發滿**。
   排程時間若早於美東 8 點會拿到大量空白趨勢。
   ⚠️ **但發佈時間會整天延遲**：2026-08-01 實測全部場次到美東中午 12 點才發佈，
   當天四輪排程（最晚落在美東 11 點）全部撲空、網站整天 0 推薦。因此加了 UTC 16:17/17:17 兩輪補救。
   判斷方式：趨勢未發佈時 covers 頁面的 `<section id="trends">` 內是
   `"Check back shortly to see trends for each team."`，且整段只有約 583 字元。
   **同一頁的 "Recent Form" 趨勢（`single-form-trend`）與 ROI Trends 是不同來源，前者有資料不代表後者有。**
   要區分「covers 沒發」還是「我們解析壞了」，最快的方法是抓一場**昨天已完賽**的比賽頁——
   已完賽頁的趨勢會長期保留，若它還解析得出趨勢標題就代表解析邏輯沒問題。
   ⚠️ **更快的判別法見第 10 點**（比對 trends 段落長度與 `Check back shortly` 字樣）；
   2026-08-22 就是解析壞掉卻被誤判成「沒發佈」，白等了一整晚。
3. **樣本數有四種寫法，四種都要解析**（2026-08-26 補齊）：

   | 寫法 | 例子 | 解讀 |
   |---|---|---|
   | `in N of their last M` | `in 32 of their last 45 games` | N / M（主要格式）|
   | `in their last M` | `in their last 6 games` | **M / M（全勝）** |
   | `not ... in any of their last M` | `not hit ... in any of their last 8 games` | **0 / M（掛零）** |
   | 戰績 `7-2` | | 7 / 9（少數）|

   ⚠️ **沒有分子不代表「無法解析」，而是全勝**。舊版只認第一與第四種，後兩種會讓
   `wins`／`sample` 都是 `None`，而 `None` **會通過 `usable_trends` 的 `MIN_TREND_SAMPLE`
   那道關卡**（那是刻意留給真正解析不出來的句子的，見該處註解），再被
   `success_lb`／`fail_lb` 的先驗 `wilson_lower_bound(2.5, 5)` = **25.2%** 壓低。
   後果**兩個方向都錯**：

   - 樣本 <8 的本該整筆排除，卻掛著一個沒有意義的分數留在單場卡片上
     （實測 90 天出現 6 筆，畫面顯示「過盤: 19/30 (63%) + 紀錄不明」）
   - 樣本 >=8 的本該正常計分，卻被壓成 25.2%
     （2026-08-23 守護者獨贏：對手掛零 0/8 應為 83.0%，整筆分數 34.5% → 應是 **63.4%**，
     當天 Top 5 門檻 58.0，等於**漏掉一筆本該上榜的推薦**）

   比對順序為 `of_last` → `not_any` → `in_last` → `record`：明確寫出分子/分母的優先，
   最後才輪到寬鬆的 `\d+-\d+`（它會誤中年份之類的字串）。
   實測完整歷史 7348 句：原本成功的 7296 句零回歸、新解析 52 句、仍無法解析 0 句。
4. 賽事列表的開賽時間在 `gamebox-time` div 內，格式如 `"Fri, Jul 10 6:40 PM ET"`
   （舊 class `preGame-time` 已不存在）。
5. **已打完的比賽頁給簡稱隊名（"Cubs"），未開打的給全名（"Chicago Cubs"）**——
   前端翻譯字典兩種 key 都要有。
6. 隊伍代碼（`data-home-team-shortname`）：az/ath/laa/lad/sd/sea/sf 為太平洋時區、
   chc/chw/hou/kc/mil/min/stl/tex 中部、col 山區、其餘美東。亞利桑那不實施夏令時間。
7. **盤口表是「各家運彩公司報價」的清單，不是單一盤口**（2026-08-20 實測）。
   同一個 `Game Line - Run Line - FT` 標籤底下有多列，**會混入 alternate run line**
   （實測出現過 ±2.0／±2.5／±3.0），而且個別莊家會把主客兩欄的正負號寫反
   （實測 williamhill：betway／bet365 給 `+1.5/-1.5`，它給 `-1.5/+1.5`）。
   **MLB 標準讓分盤永遠是 ±1.5**，取到別的數字一定是解析錯誤。
   ⚠️ 這比顯示錯字嚴重：covers 自己的 Run Line 趨勢統計是以標準 ±1.5 算的，
   標成「受讓 3.0」等於叫使用者去下一個和趨勢完全不同的盤口；正負號判反則會讓
   Recent Form 的「直接獲勝是否蘊含過盤」推論整個相反。
   解法見 `parse_run_lines`：只承認 ±1.5、以多數決決定讓分方，判不出來就回 None。

   ⚠️ **但「不承認那個數字」不等於「連方向都不知道」**（2026-08-28 修）。
   使用者問「白襪今天是讓還是被讓」，網站寫「買白襪 讓分」——那個方向是猜的，
   而且猜錯邊。covers 那場的讓分區段**整段只有 ±2.5，沒有標準的 ±1.5**，
   於是 `parse_run_lines` 回 None，再被 `determine_side_term(None)` 預設成「讓分」。
   實際上客隊白襪是 +2.5＝受讓方，正確答案是「受讓 1.5」。

   ±2.5 的熱門方就是 ±1.5 的熱門方，**正負號本身已經指出方向**。所以改成兩層：

   | 層 | 用途 | 資料 |
   |---|---|---|
   | 第一層 | 決定方向（不變） | 只用標準 ±1.5 多數決 |
   | 第二層 | 第一層判不出來時才跑 | 用 alternate line 的正負號決定方向 |

   **數字一律以標準 1.5 呈現**——使用者在 playsport 下的本來就是 1.5，
   把 2.5 印出去等於叫他下一個和趨勢不同的盤口。第二層只在第一層回 None 時執行，
   所以原本判得出來的場次逐字不變。

   判別方式（畫面上唯一看得出來的症狀）：**推薦文案少了數字**——
   正常是「讓 1.5」「受讓 1.5」，猜出來的是光禿禿的「讓分」。
   `diagnose_run_line()` 會在判不出方向時印出標籤出現次數、區段數、
   兩層的票數與各家報價，用以分辨「標籤被改名／只剩 alternate line／多數決打平」。
8. **大小分盤口要取頁面頂端摘要表的 `Total` 列**，不要取各家報價的第一列。
   兩者實測 9/9 場一致，但摘要表在「沒有 `Game Line - Total - FT` 區段」的頁面上仍然存在
   （實測 368781 就是這種頁，舊寫法回 None）。各家報價差距可達一整分（實測 6.5／7.0／7.5 並存），
   **千萬不要改成取多數決**——實測多數決有 3/9 場與 covers 顯示的主盤口不符。
9. **賽事列表在比賽開打後就沒有開賽時間**：`gamebox-time` 會換成比分／局數，
   該場 `game_time` 變成 `"None"`（實測全歷史 17.3%，最早那輪 UTC 11:17 高達 56%）。
   ⚠️ **單場頁面的 `startDate` 只有賽前才有**——實測進行中的頁面是 `InProgress`、
   已完賽的頁面兩者皆無。所以退回機制是三層，缺一不可：
   1. 賽事列表的 `gamebox-time`（正常情況）
   2. 單場頁面 schema.org 的 `"startDate"`（UTC 絕對時間，`parse_start_time_et()` 換算成 ET，
      實測 9/9 場與列表值完全一致）——只救得到**尚未開打**的場次
   3. `load_known_game_times()` 沿用**上一輪 index.html 已抓到**的時間——救進行中／已完賽的場次。
      每天跑六輪，早幾輪（美東 07:00~11:00）一定在賽前，所以後面幾輪一定有東西可沿用。
      只在既有頁面的賽事日期與本次相同時才沿用，避免把昨天的時間帶到今天。
   三層都沒有時前端顯示「開賽時間未提供」，不要再讓 `None` 直接印到畫面上。
10. **covers 會改 HTML 標記，而且改了不會有任何跡象**（2026-08-22 實測）。
    那天把 `<h4 class="High">` 改成 `<h4 class="trend-article-heading High">`，
    舊 regex 寫成 `class="(High|Low)"` 要求**完全相等**，15 場全部解析出 0 條。
    ⚠️ **解析壞掉與「趨勢尚未發佈」的症狀完全一樣**——都是 0 條、都不報錯。
    差別只在頁面本身：

    | | 趨勢未發佈 | 解析壞掉 |
    |---|---|---|
    | `<section id="trends">` 長度 | 約 583 字元 | 4~5 萬字元 |
    | `Check back shortly` | 有 | 無 |

    現在 `parse_matchup_details` 會在「趨勢區有 `Units` 字樣卻抓到 0 條」時印出
    警告與實際的 h4 標籤，不會再靜默失敗。**比對 class 一律要容許多個 class token**
    （寫成 `class="(?:[^"]*\s)?(High|Low)(?:\s[^"]*)?"`），新增任何 class 比對請比照。

    2026-08-26 稽核後，以下**全部**改成容許多個 token，改版時先檢查這份清單：

    | 位置 | 用途 | 壞掉的後果 |
    |---|---|---|
    | `team-score` + `d-xl-inline-block` | 抓比分 | **戰績永久遺失**（見下）|
    | `odds` + `upper-block`（2 處）| 讓分／總分線 | 退回不帶數字 |
    | `other-odds-label`（3 處）| 盤口分段 | 同上 |
    | `single-form-trend` | 近期走勢 | 該區空白 |

    ⚠️ **放寬 class 比對時不可連語意條件一起放掉**。第一版把比分規則放寬成
    「只要有 `team-score`」就差點出事：`d-xl-inline-block` 是用來區分桌機版的，
    拿掉之後若一場抓到 4 個數字（手機／桌機重複渲染），`nums[0]`／`nums[1]`
    可能變成同一隊的兩個值，**會靜默記下錯的比分，比原本的 bug 更糟**。
    正確做法是「要求原本所有的語意 token，只容許額外或重排」——
    寫成 `class="(?=[^"]*\bteam-score\b)(?=[^"]*\bd-xl-inline-block\b)[^"]*"`。
    賠率同理（少了 `upper-block` 會被 `lower-block` 的數值干擾）。

    ⚠️ **比分那條路徑要特別小心**：抓不到不會報錯，只會讓 `backfill_results`
    靜默記不到結果、戰績停止累積；而回填只回看 `RESULT_LOOKBACK_DAYS`(7) 天，
    超過就再也補不回來。`fetch_final_scores()` 現在會在「有 Final 場次卻抓到
    0 個比分」時印警告，並在「一場抓到超過 2 個比分」時跳過該場——
    寧可不記，也不要記錯的。

    ⚠️ 更重要的是**推論的教訓**：當天曾用「近期走勢有 120 條」與「昨天同一份程式正常」
    推論成 covers 沒發佈——**兩個都推不出 ROI Trends 的狀態**（第 2 點就寫了那是不同來源），
    害使用者白等三輪排程加三次手動觸發。使用者說「我去看 covers，有看到 trends 呀」
    才是決定性的證據。**使用者的直接觀察勝過我方的間接推論，先驗證再下結論。**

## 推薦演算法（scrape.py）

- 排序統一用**過盤命中率的 Wilson 90% 信賴下界**（z=1.28），顯示「過盤: 32/45 (71%) | 保守 61.8%」。
  小樣本高命中率會被下界自然壓低，不需手調參數。
  **UI 用語是「保守命中率」（徽章縮寫「保守」）**——使用者反映「下界」不直覺（2026-07-11 改），
  程式註解仍用統計術語「下界」沒關係，但使用者看得到的文字一律用「保守命中率」。
- 樣本 < 8 場（`MIN_TREND_SAMPLE`）的趨勢不參與推薦媒合。
- 勝負盤分數 = (強隊過盤率下界 + 對手不過盤率下界) / 2；大小分 = 兩隊趨勢下界平均。
- 判讀門檻（網頁上有說明列 + 顏色）：**≥55% 綠/強、50~55% 黃/普通、<50% 灰/忽略**。
- 首五局 (F5) 與 Team Total 趨勢刻意排除，只做全場市場。
- 下午場判定以**主場當地時間**（當地 17:00 前開打），非 ET。

### Recent Form 中譯：條件子句要用 regex，不能只靠片語表（2026-08-20 修）

`RF_PHRASES` 是純字串替換表，原本只寫死了幾個條件子句（`with 5 days of rest`、
`after allowing 2 runs or less...`）。但這些子句帶**可變數字**，寫死必然漏：
實測歷史 1783 句有 235 句（13.2%）夾著英文，換算成畫面上**每天約 15% 的走勢項目**。

更糟的是「翻一半」——表尾有 `('home', '主場')`／`('road', '客場')` 這種單字規則，
會啃掉英文子句裡的字，產生：

    原文  ...home games vs. a team with a road winning % of greater than .600
    舊譯  ...（主場 vs. a team with a 客場 winning % of greater than .600）

所以新增 `RF_PATTERNS`（regex），並且**一定要在 `RF_PHRASES` 之前套用**。
涵蓋：N runs or more／N days of rest／WHIP greater than X／winning % of .XXX／
winning|losing home|road record／following a loss|win／road trip of N days／
主審姓名／MLB 城市名（`RF_CITIES`，只收城市避免把人名誤譯）。
實測 235 → 2 句殘留（一句是主審人名 `Gibson III`，本來就該留英文；
一句是 covers 自己漏了隊名的 `are 7-0 in Lopezs last 7...`）。
移除的 7 條寫死片語經逐句比對確認譯文完全相同，零回歸。

⚠️ **同一個片語 covers 可能有多種寫法，片語表只收到其中一種就會翻一半**
（2026-08-26 修）。`RF_PHRASES` 收了 `as a home underdog`，但 covers 有時
**省略 `as`**（`in Scherzers last 5 starts a home underdog`，實測 31 次、跨 18 天）。
少了規則，`home` 就被表尾單字規則啃成「主場」，其餘留英文 →「（a 主場 underdog）」。
已改為 `RF_PATTERNS` 的 `(?:as\s+)?an?\s+(?:(home|road)\s+)?(favorite|underdog)s?`，
六種組合一次涵蓋。**新增片語時先想「covers 會不會少寫一個虛詞」，能用 regex 就別寫死。**

### Recent Form 趨勢：只顯示，**永遠不計分**（2026-08-01 加入）

covers 每場另給 8 條 Recent Form（`<section id="form_trends">` 內的 `single-form-trend`），
與 ROI Trends 是不同來源、不同區塊。2026-08-01 實抓 64 條的統計：樣本數 4~9 場，
**64 條裡有 60 條是 0 敗**。

**市場涵蓋範圍（重要）**：實抓 120 條中，大小分 84 條、球隊**直接勝負** 36 條，
**完全沒有讓分／ATS**（120 條裡零筆出現 spread / ATS / run line / cover / 1.5 字樣）。
該區是扁平的 `<ul>`，沒有盤口標籤也沒有分隊分組，市場只能從句子本身判斷。

讓分盤的標記是**由直接勝負推得**，強度依 `spread_side` 分兩級（2026-08-02 加入）：
- **受讓 +1.5**：直接獲勝**必定**過盤（獲勝是過盤的真子集），邏輯上成立，視為有效佐證。
- **讓分 -1.5**：直接獲勝只是必要條件，還需贏 2 分以上，標為「較弱」並在文案寫明。
  依據：以本專案 30 隊實測，獨贏命中率與讓分過盤率**相關係數僅 0.407**
  （獨贏均 56.0%、讓分均 52.4%），個別落差可以很大（水手 64%→26%、費城人 60%→38%），
  不足以把兩者等同看待。

**為什麼不能列入排序**（這是加功能時最容易犯的錯）：
- 套用現行 Wilson 下界，64 條**全部** ≥55%（範圍 57.5%~84.6%），而真正有厚度的
  ROI 趨勢 32/45 才 61.8%、29/50 只有 49.0%。一旦混入排序，AI Top 5 會被連勝紀錄全面佔滿。
- 下界修正的是「樣本小」，前提是隨機抽樣；但這些是 covers **在同一場比賽試很多條件切法**
  （as a favorite / vs. a right-handed starter / after allowing 2 runs or less…）後挑出的全勝切片。
  選擇偏誤對 Wilson 是隱形的。丟硬幣 5 連中的機率就有 3%，試 100 種切法必然冒出好幾條。
- 資料本身即為佐證：8 場裡有 5 場**同時**出現 Over 與 Under 的全勝紀錄，自相矛盾。

**因此的設計**：`parse_recent_form()` 只做解析與中譯，`recent_form_lean()` 算大小分傾斜且
**淨差 ≥3 才承認有方向**（因為多數場次是分歧的）；獨贏比對用 `win_team` 欄位，**淨差 ≥2** 才標記。
前端用途都只是顯示，不改分數也不改排序：
- 單場卡片的「📈 近期走勢（僅供參考）」清單
- 單場推薦上的完整標記：`recentFormFlag`（大小分）、`recentFormSideFlag`（獨贏；讓分回傳空字串）
- AI Top 5 與左右兩個 Top 5 清單的精簡藥丸標記：`recentFormPill` → `recentFormStatus`，
  後者以 `matchup_id` 反查 `allMatchups`，**與單場卡片共用同一套門檻**，改規則只需改一處。

- **「差一點進 Top 5」向隅區**（`collectNearMisses`）：走勢同向但落榜的推薦，
  只在「與該類 Top 5 最低分差 ≤3 分（`NEAR_MISS_MARGIN`）」**且**「分數 ≥50%
  （`NEAR_MISS_MIN_SCORE`，網站自訂的可忽略門檻）」時才列出，最多 5 筆。
  兩道關卡缺一不可——落榜本來就代表命中率證據較弱，只有分差小到沒有實質意義時，
  走勢同向才值得補救；否則就變成用小樣本連勝把弱推薦拉上檯面，等於偷偷影響排序。
  無符合者整區隱藏。2026-08-02 當天 9 筆同向中，3 筆已在 Top 5、2 筆低於 50% 被擋，實列 4 筆。

附帶好處：covers 的 ROI Trends 開天窗時，Recent Form 通常還有資料。
注意 Top 5 常常整排都沒有標記，這是正常的——多數推薦是讓分盤（不適用），
獨贏則常見 0比0 或 1比1 未達門檻。要確認功能是否還活著，用瀏覽器主控台跑
`recentFormStatus(topTotals[i])` 看回傳值，不要只看畫面有沒有標記。

解析細節：句型高度模板化，中譯用 `RF_PHRASES` 片語表（由長到短比對）。幾個坑——
單位字 `games`/`starts` **不一定在開頭**（"interleague games"）；投手/主審用所有格但
covers 省略撇號（"Gausmans" = Gausman，`starts` 代表投手、`behind home plate` 代表主審）；
球隊簡稱輸出成 `@@Rays@@` 佔位符，由前端 `renderRecentFormText()` 依語言設定翻譯，
由前端 `renderRecentFormText()` 統一替換成中文隊名。

### Top 5 精選清單的組成規則

- **分數下限 `TOP_PICK_MIN_SCORE = 50`**（2026-08-05 加）：低於 50% 的推薦不進任何精選清單。
  加這道門檻的原因是自相矛盾——網站說明列自己寫「<50% 僅供參考」，向隅區也用 50 擋，
  唯獨正榜沒有下限：實測 27 天有 5 天讓 <50% 的推薦上榜（7/14~7/16、7/20、7/23，最低 46.8%），
  7/14~7/16 甚至整個 Top 5 只有那一筆 49%，等於逼使用者去下系統自己標為可忽略的注。
  **寧可列不滿 5 筆，甚至整區掛零**（前端有專屬空狀態文案，不會整區隱藏）。
  前端 `TOP_PICK_MIN_SCORE` 由 Python 常數注入、`NEAR_MISS_MIN_SCORE` 直接引用它，
  改門檻只需改 Python 那一個地方。

- **同場同隊只留分數最高的一筆**（`dedupe_same_team_picks`）。「買 X 獨贏」與「買 X 讓 1.5」
  是同一個看法的兩種風險版本，兩筆都進榜等於浪費名額、也讓人誤以為有 5 個獨立標的。
  修正前實測 17 天：AI Top 5 有 10 天出現同場重複、勝負 Top 5 有 8 天出現同隊重複。
  單場卡片不受影響，仍完整顯示所有推薦。
  **向隅區（`collectNearMisses`）也要套同一條去重規則**——它走的是另一條程式路徑，
  只擋掉「已在 Top 5 的隊伍」不夠，同隊的兩種盤口會在那裡並列（2026-08-02 實際發生過）。
- **大小分「不需要」對應的去重／互斥處理**——別再加一次（2026-08-05 加過、08-06 移除）。
  單場的 `double_positive` 結構上最多 1 筆：`analyze_betting_recommendations` 的
  「全場大小分避碰」三個分支**都只 append 一筆**，源頭就決定好方向了。
  實測 F5 排除後 67 天 / 878 場，`double_positive` 只有 0 或 1 筆，從未出現 2 筆。
  當時加的 `dedupe_totals_picks` 不只永遠不執行，**在唯一可能被喚醒的情境下還是錯的**：
  歷史上單場 2 筆的 14 場（2026-05-29/30，F5 尚未排除）是
  `買 全場小分 (Game Under)` ＋ `買 首五局大分 (F5 Over)`——**不同市場、可同時成立**，
  但它只看 `direction` 不看 market，會判為互斥把兩筆都丟掉；去重鍵 `(matchup_id, direction)`
  也會把「全場小分」與「首五局小分」誤當重複。
  **要改大小分的取捨邏輯就改上游那個避碰，不要在下游疊第二層。**
- **AI Top 5 只排除「真正互斥」的同場推薦**（`picks_are_contradictory`），以分差建模：
  獨贏需 ≥1 分、讓 1.5 需 ≥2 分、受讓 1.5 需 ≥-1 分，兩隊區間無交集才算矛盾。
  舊版用「押到不同隊就整場排除」，實測 17 天內 14 場被排除、**其中 7 場其實相容**
  （最典型：買 A 獨贏＋買 B 受讓 1.5，A 贏 1 分時兩邊同時成立）。
- **排序仍只看保守命中率；近期走勢僅在分數完全相同時決勝**（`recent_form_agreement` +
  `rank_key`）。原因：Top 5 邊界常是平手（17 天中 11 天第 5 與第 6 名相差 <1 分、2 天完全同分），
  而 `sorted` 是穩定排序，同分時實際上由「covers 賽事列表順序」決定名次，那是任意的；
  以弱訊號取代任意順序期望值不會更差。**低分永遠不會被走勢拉到高分前面**，
  否則畫面上會出現「分數較低卻排在前面」的矛盾。
  該函式的門檻與前端 `recentFormStatus()` 相同，改動請同步兩處。

### 詳細趨勢區的中譯（`translate_trend_text`，2026-08-05 加）

ROI Trends 原文以前是全站唯一沒中譯的區塊。**這裡刻意不用 `RF_PHRASES` 那種片語表**——
實測歷史 4224 句只有 78 種骨架，而骨架是「否定 / only / 盤口 / 樣本寫法 / 主客場」
五個**正交**欄位的組合；片語表要窮舉組合會漏，拆欄位的結構化 regex 才蓋得完。
實測翻譯率 100%，且樣本數字、隊名、only／not 語意全部保留。

- 盤口名用**完全比對**（`TREND_MARKET_ZH`），不是子字串比對，否則 `Team Total Over`
  會被 `Game Total Over` 的規則吃掉。沒收錄的盤口一律回傳 `None`。
- 比對不到就回 `None`，前端 `renderTrendText()` 退回顯示英文原文——**寧可沒翻，不要翻錯**。
- 隊名輸出 `@@隊名@@` 佔位符，與 Recent Form **共用** `renderRecentFormText()` 替換，
  由前端 `renderRecentFormText()` 統一替換。有 `text_zh` 就用它，翻不出來才退回
  `text`（covers 原文）。
- 樣本寫法有兩種：`in N of their last M`（一般）與 `in their last M`（全數命中，
  沒有 N）；另有 `not ... in any of their last M`（掛零）。三種都要處理。

## 戰績累積（history.json，2026-08-21 加入）

covers 只給賽前趨勢，比賽結果不在專案裡。以前想回答「推薦到底準不準」得臨時去 covers
把幾十天的計分板一天一天抓回來，慢、多打擾對方，而且算完就沒了、無法累積。
現在每輪把「當天的推薦」與「前一天的比分」寫進 `history.json`（一天約 15 場、整季不到 1 MB）。

- **只用來顯示，不參與任何排序或篩選。** 理由和 Recent Form 相同：拿三位數的樣本回頭
  調參數，過擬合的風險遠大於訊號。要動排序前先確認樣本厚度。
- 賽果用 `fetch_final_scores()` **一天一次請求**抓計分板，不是一場一次（差 15 倍請求量）。
  `backfill_results()` 每輪最多補一天、只回看 `RESULT_LOOKBACK_DAYS`(7) 天——
  延賽／取消的場次永遠不會有比分，沒有上限的話會每輪重抓。
- 同一天會被後面幾輪覆蓋，這是刻意的：使用者晚上看到的是最後一輪，統計就該以那份為準。
- **回填只從 2026-07-10 起**。那天修掉了「分析已完賽比賽」的 bug——實測那之前
  **39.6%** 的場次在頁面標示日期之前就已經打完（修好後只剩 5.5%），
  那段期間的「命中率」不是預測能力，混進去會讓網站上的數字失去意義。

### ⚠️ 盤口 key 一定要走 `_market_key()`

Top 5 給的是 `market_type`（`"受讓 1.5"`、`"Over (大 7.5)"`），單場推薦給的是
`market_zh`（`"受讓 1.5"`）。**兩邊必須用同一個正規化函式**。
第一版各自處理，Top 5 用 `split(' ')[0]` 切出 `"受讓"`、單場用 `"受讓 1.5"`，
比對永遠不成立 → **讓分盤的 `top5` 旗標整批沒被標到**（實測 108 筆裡一筆讓分都沒有）。
而讓分剛好是命中率最低的盤口（約 38%），統計因此被系統性高估成 55.3%，
修好後是 52.8%。這種錯不會報錯，只會讓數字偏樂觀。

### 讓分判定以「畫面上當時顯示的盤口」為準

2026-08-21 之前顯示過 alternate run line（`受讓 2.0` 之類，見上面 covers 行為第 7 點）。
`_judge_pick()` 用 `pick['line']`（未標示則為標準 1.5）判定，並保留 `market_raw` 供稽核。
整數盤口會有和局（`受讓 2.0` 輸剛好 2 分是和局），回 `None` 不計入命中率。
實測受影響的 Top 5 只有 2 筆、兩場都是輸 1 分，兩種算法結果相同，但寫法不該留著含糊。

### 回填舊資料時的兩個欄位陷阱

- `double_positive` 的 **`direction` 是 2026-08-01 才加的欄位**，之前的資料沒有。
  只看 `direction` 會讓回填時整批大小分推薦被跳過（實測少了 68 筆 Top 5，
  總數從 188 掉到 159、命中率被算成 52.8% 而非 51.6%）。
  `_pick_rows()` 已退回用 `market_type`（`"Under (小 8.5)"`）判斷。
- **`rf_agree` 只存在於 `top-ai-data`**，單場的 `opposing_trends` / `double_positive` 都沒有，
  所以要在走訪 Top 5 清單時順手帶進來。三態 `True/False/None` 要原樣保留：
  `None` 是「沒有明顯方向」，和「那天還沒有 Recent Form 資料」不同——後者不會有 `rf` 這個 key。

### ⚠️ 近期走勢的三態不可以合併成兩態

`rf_agree` 是 `True/False/None`（同向／反向／沒有明顯方向）。
第一版把 `False` 和 `None` 併成「未同向」，得到的結論是**相反的**：

| 分組 | 命中率 |
|---|---|
| 走勢同向 | 48.6% (17/35) |
| 走勢反向 | 38.5% (5/13) |
| 走勢無方向 | 65.2% (30/46) |
| ~~合併成「未同向」~~ | ~~59.3% (35/59)~~ |

合併後看起來像「不同向反而比較準」，但真正的順序是 **反向 < 同向 < 無方向**。
而且「無方向」的優勢幾乎全來自獨贏 13/15（86.7%）這一段運氣——排除獨贏後
只剩 54.8% vs 43.5%。三組兩兩比對 z 值最大也只有 1.73，全都未達顯著。

**別拿這個去調排序。** 目前唯一站得住的結論是「看不出走勢有加分效果」，
這和當初刻意不把 Recent Form 列入計分的判斷一致。

### 戰績分析的三個結論（2026-08-28，222 筆可判定 Top 5）

⚠️ 這些**只是觀察，還沒拿去改任何排序**。記在這裡是為了擋掉「想當然耳」的直覺。

**一、照分數門檻篩選沒有用。**

| 區間 | 命中 | 命中率 |
|---|---|---|
| 🟢 ≥55（強）| 89/170 | 52.4% |
| 🟡 50~55（普通）| 25/47 | 53.2% |

綠色比黃色**低** 0.8 個百分點，z = −0.10。**別再建議「只跟高分的」**——那句話很直覺但資料不支持。
（≥61 分那格是 19/28 = 67.9% 看起來突出，但 z = 1.72 未達顯著，而且是切了五個區間才挑出來的，
典型的多重比較。不要拿它調排序。）

**二、真正的差異在盤口，而且有機制。**

| 盤口 | 命中 | 命中率 |
|---|---|---|
| 受讓 1.5 | 19/31 | 61.3% |
| 獨贏 | 49/85 | 57.6% |
| Under | 32/62 | 51.6% |
| Over | 4/9 | 44.4% |
| **讓 1.5** | 13/35 | **37.1%** |

⚠️ 先驗證過**不是判定寫錯**：35 筆逐筆比對比分，`_judge_pick` 零錯誤。
淨勝分分佈顯示押注方其實贏了 19 場（54.3%），但**有 6 次只贏 1 分**——是 −1.5 那條線吃掉的。

**三、`Run Line` 趨勢的語意與我們的用法不符（最重要）。**

比較「趨勢宣稱的過盤率」與「實際結果」：

| 盤口 | 筆數 | 趨勢宣稱 | 實際 | 落差 |
|---|---|---|---|---|
| 獨贏 | 117 | 70.2% | 54.7% | −15.5 |
| 受讓 1.5 | 48 | 67.5% | 58.3% | −9.1 |
| **讓 1.5** | 54 | 69.1% | **35.2%** | **−33.9** |

三者都往下掉是正常的（covers 的趨勢是切片挑出來的，必然回歸）。但讓分掉了兩倍多，原因是
**covers 的 "covered the Run Line in N of their last M" 是混合的**——那 M 場裡該隊有時是
−1.5 讓分方、有時是 +1.5 受讓方，兩者難度天差地遠。把混合率套到「這場他是 −1.5 讓分方」
就是類別錯誤：

- 押 **受讓 +1.5** 時，混合率**低估**了容易度 → 落差小（−9.1）
- 押 **讓 −1.5** 時，混合率**高估**了 → 落差巨大（−33.9）

不對稱的方向完全符合這個解釋，不是事後湊出來的。

**後果**：AI Top 5 是跨盤口用同一個分數排序的，而讓分的分數被系統性高估約 20 個百分點，
所以**讓分在 Top 5 裡被過度代表**（222 筆裡佔 35 筆）。要動這裡的話，
問題是「跨市場的分數不可比」，不是「門檻該調幾分」。

### 讓分方向曾被猜錯，但戰績幾乎沒受影響（2026-08-28 查證）

上面那個「預設讓分」的 bug 在 history.json 期間（7/10 起）產生 4 筆方向靠預設值的推薦，
其中**只有 1 筆進過 Top 5**（8/20 釀酒人，另外 3 筆是單場卡片上的推薦，不列入統計）。

那場釀酒人主場 7-4 贏 3 分，**讓 1.5 與受讓 1.5 都算過盤**，所以總計完全不變：

| | 總計 | 讓 1.5 | 受讓 1.5 |
|---|---|---|---|
| 現況 | 117/226 (51.8%) | 13/36 (36.1%) | 19/31 (61.3%) |
| 若那筆其實是受讓 | 117/226 (51.8%) | 12/35 (34.3%) | 20/32 (62.5%) |
| 若那筆整筆不出 | 116/225 (51.6%) | 12/35 (34.3%) | 19/31 (61.3%) |

差異只在「歸到哪個盤口欄位」，最大 1.8 個百分點，且方向是讓分更低、受讓更高——
**不影響「讓分是最差盤口」這個結論**。當時的真實盤口已無從查證（頁面過去了），
所以不回頭改資料，記在這裡即可。

### 頁面呈現

底部 `render_track_record()` 產生的摺疊區，**預設收合**——網站在台灣 22:00 的任務是
「今天買什麼」，戰績是事後參考，放頂端會把推薦擠下去。幾個刻意的決定：

- **分母和百分比一樣大**：17 筆的 41% 和 170 筆的 41% 是兩回事，只寫百分比會把雜訊當訊號。
- **不顯示保守命中率**（2026-08-21 使用者要求拿掉，畫面太雜）。`compute_track_record()`
  仍然算出 `lb` 欄位放在資料裡，要恢復顯示只要把 `render_track_record()` 的 span 加回去。
- **「已標記 Top 5」與「可判定」是兩個數字**，別混用。實測 198 筆標記只有 188 筆能判定，
  差在：今天的比賽還沒打（5）、covers 沒給總分線所以推薦沒有盤口數字（3）、
  延賽沒有比分（1）、整數盤口和局（1）。講「大分有 11 筆」和表上顯示 `4/8` 都對，
  但它們不是同一件事——表上是可判定的筆數。
- 展開高度用 `scrollHeight` 寫成 inline style（`toggleTrackRecord()` / `syncTrackHeight()`），
  CSS 那個 2000px 只是無 JS 時的保底——和摺疊卡片踩過的坑相同，別改回固定值。
- 表格用 `.tr-table-wrap` 的 `overflow-x: auto` 自己捲，不要讓它撐爆頁面。

## 先發投手（statsapi.mlb.com，2026-08-28 加入）

單場卡片的開賽時間下方顯示兩隊先發投手與當季成績。**只顯示，不進評分或排序。**

    ⚾ 先發  辛辛那提紅人 Rhett Lowder  5-8 · ERA 5.13 · WHIP 1.47
             芝加哥小熊 David Peterson  7-7 · ERA 5.17 · WHIP 1.50

**為什麼不計分**（和 Recent Form、戰績同一個立場，但理由不同）：先發投手是盤口
**開盤前就掛牌、早已定價**的資訊。covers 的趨勢之所以還有一點價值，是因為它是切片統計；
先發投手則是市場人人都看得到的公開資訊，拿它去調分數不會帶來優勢，只會讓排序被一個
沒驗證過的權重污染。它的用途是讓使用者看到推薦時有背景資訊自己決定「這筆要不要跳過」。

### covers 沒有先發投手，只能另外抓

2026-08-28 實測 picks 頁面：`probable` 出現 **0 次**、`starting pitcher` **0 次**、
含 `pitcher` 的 class **0 種**（`pitcher` 那 40 次都在 Recent Form 的句子裡）。
所以「零額外請求」這條路走不通，必須用 statsapi。

statsapi 免金鑰、回 JSON、標準庫就打得到，維持專案零外部依賴的設計。
從 Actions runner 實測 HTTP 200、45 KB、**0.3 秒**。
⚠️ **雲端 session 的 egress proxy 擋掉 statsapi.mlb.com**（CONNECT 403），
和 covers 一樣，任何驗證都只能透過 Actions 跑。

### ⚠️ statsapi 對不認得的 hydrate 是「靜默忽略」

`schedule?hydrate=probablePitcher(stats(group=[pitching],type=[season]))` **不會**帶回成績，
只回 `{id, fullName, link}`——不報錯、不警告，就是安靜地少一半欄位。
這與 covers 改標記是**同一族的失敗**：拿不到資料而畫面照常。

因此成績改用 `people?personIds=...&hydrate=stats(...)` 端點另抓一次（整輪只多一個請求），
且**兩種 hydrate 寫法都試**（帶 `season=` 與不帶），兩種都拿不到成績時印警告。
新增任何 hydrate 參數時記得：**要自己檢查回傳有沒有那個欄位，不能假設沒報錯就是成功。**

### 降級行為（三層，缺一不可）

| 情況 | 行為 |
|---|---|
| statsapi 整個抓不到 | `pitchers` 為空 → 每場 `pitcher_a/b` 皆 None → **整區不輸出** |
| 單邊沒公布先發 | 該列顯示「未公布」，另一隊照常 |
| 有名字沒成績 | 只顯示名字，**絕不把 None 印到畫面上**（`format_pitcher` 逐欄位判斷）|

網站在使用者看盤時段必須照常出推薦，先發投手掛掉不能拖垮當日產出。

### 隊名比對與它的驗證方法

covers 給全名（`"Chicago Cubs"`），偶爾還會把隊名寫兩次（實測 **`"Athletics Athletics"`**）。
`fetch_probable_pitchers` 對每隊建三個 key：`name`／`clubName`／`locationName + clubName`，
`lookup_pitcher` 先試完全比對，再退回包含關係。

⚠️ **驗證比對是否錯位，看「有沒有重複的投手名字」**，不是看比對成功幾筆。
比對錯位的症狀是某位投手同時掛在兩隊名下，而總筆數完全正常。
2026-08-28 實測：statsapi 給 28/30 隊有先發（2 隊未公布），畫面顯示 28 位、
**30 隊皆不重複、投手名零重複** → 比對率 28/28。

## GitHub Actions 排程（.github/workflows/scrape.yml）

- 每天 6 輪：UTC 11:17/12:17/13:17/15:17（台灣 19:17~23:17）＋ 補救輪 16:17/17:17（台灣 00:17/01:17）。
  多輪的原因：(a) Actions 排程實測延遲 **1~2.5 小時**（2026-08-01 實測 57~101 分鐘，比原本記的 2~2.5 小時短）；
  (b) 後面輪次用更完整趨勢刷新；(c) 補救輪專門處理 covers 延遲到美東中午才發佈的日子。
  目標是保證台灣 22:00 前必有當日資料，最差情況也在美東開賽前補齊。
- 有 `concurrency` group 防止延遲輪與準時輪同時執行撞 push；push 前先 `git pull --rebase`。
- 內容沒變就不會產生 commit（`Commit and Push Changes` 步驟以 `git diff --cached --quiet` 判斷，
  並把結果寫進 `steps.commit.outputs.changed`）。
  ⚠️ 副作用：趨勢連續多輪都空白時，輸出位元組完全相同 → 不 commit → Pages 不重新部署，
  **網站看起來像「整天沒更新」**。要判斷是否真的沒跑，看 Actions 執行紀錄而不是 commit 紀錄。

### ⚠️ 新增輸出檔時，`Commit and Push Changes` 的 `git add` 一定要跟著加（2026-08-21）

`scrape.py` 每輪改寫 **兩個** 檔案：`index.html` 與 `history.json`。
`history.json` 是 2026-08-21 加的，但當時只把它 commit 進版控，
**忘了改 workflow 的 `git add index.html`**。後果不是少存資料而已：

    git add index.html          # history.json 留在工作區沒被暫存
    git commit -m ...           # 成功
    git pull --rebase origin main
    → error: cannot pull with rebase: You have unstaged changes.
    → exit 128，整輪 job 失敗

當天 UTC 11:44／13:13 兩輪全掛（runs 258/259），commit 只留在 runner 上沒 push，
**網站整天停在 8/20 的資料**。第二個後果比較隱形：`history.json` 永遠不會被 commit，
每輪都讀到 checkout 出來的舊檔，**戰績與賽果回填等於白算**。

現在是 `git add index.html history.json`，並在兩處 `git pull --rebase` 都加 `--autostash`——
以後若再新增輸出檔而忘了 add，至少 rebase 不會整輪失敗（但資料仍不會進版控，
**該加的還是要加**）。這也讓 `Verify Deployment` 那個 pull 不會被同樣的髒工作區擋住。

⚠️ 症狀容易誤判成 Pages 或 covers 出問題：Actions 是紅的、但爬蟲日誌一路正常跑到
「抓取與分析完成！」，錯誤在最後兩行。看 Actions 失敗要往日誌**最下面**看。

### ⚠️ Pages 部署會失敗，而且會和「內容沒變不 commit」疊成長期卡住（2026-08-06）

那天四次 Pages 部署掛了三次，兩種失敗模式：`fac6d82`／`7388057` 是 build 全過、
只有 `Deploy to GitHub Pages` failure；`dddcf72` 是 build 跑 15 分鐘被 cancelled、deploy skipped。

⚠️ **但那天 GitHub 有全站故障**（githubstatus.com 的 Actions/Pages 都是 `major_outage`，
公告寫 "Workflow runs are still failing or delayed in starting, and some queued jobs may time out"）。
**不要把「一天掛三次」當成日常頻率**——那是故障日的數字，平時大多正常。
下面的修法之所以仍該留著，理由不是「常失敗」而是**失敗的後果是無限期的**（見下），
這一點與發生頻率無關。查故障時第一件事先看 <https://www.githubstatus.com>，
別急著懷疑自己的 workflow。

單獨一次失敗還好，可怕的是和上面那條疊起來：部署失敗後，**下一輪若抓到相同內容就不 commit，
也就不會再觸發部署**，網站可以無限期停在舊頁面。那天實際發生的：

| 台灣時間 | 事件 |
|---|---|
| 21:03 | 爬蟲 commit 8/6 資料成功，Pages 部署**失敗** → 站上還是 8/5 |
| 22:37 | 爬蟲再跑，內容相同 → 不 commit → 不部署 → 站上仍是 8/5（**使用者看盤時段**） |
| 23:22 | 這輪內容剛好有變 → commit → 部署成功 → 8/6 資料才上線 |

**能恢復純屬運氣**（23:22 那輪資料剛好變了）。若整晚趨勢都沒動，會卡到隔天。

修法是 `Verify Deployment` 步驟：**只在「這輪沒有新 commit」時**抓線上頁面比對 sha256，
不一致就 `git commit --allow-empty` 逼 Pages 重新部署。幾個關鍵前提：

- **可以整檔比 hash**：實測線上頁面與 `git show <sha>:index.html` 正規化換行後**位元組相同**，
  Jekyll 是原樣輸出、不會改檔案。所以不必只比日期（只比日期會漏掉「日期對但資料舊」的情況）。
- **有 push 的那輪一定不能檢查**：部署才剛觸發，比一定不會過，每輪都會多出一個空 commit。
- 抓取時加 `?cachebust=$(date +%s)` 繞過 Fastly CDN（Pages 的 `Cache-Control` 是 600 秒）。
  沒有這個的話，剛部署完的那幾分鐘會拿到舊快取而誤判。
- curl 失敗就 `exit 0` 略過，不要因為抓不到線上頁面就亂塞 commit。
- 最壞情況（GitHub 部署持續掛）每天最多多 6 個空 commit，而且那本身就是故障訊號。
- ⚠️ **public repo 60 天無 commit 會被 GitHub 自動停用排程**——MLB 休賽期（10 月底~3 月）
  會觸發，隔年開季要去 Actions 頁面手動 re-enable。

## 日期顯示與版本號

- 標題列的日期是**美東日期**（爬蟲的 `selectedDate` 基準）。但美東下午/晚場對應台灣隔天凌晨到中午，
  使用者在台灣看到「8/1」而當地已是 8/2，會誤以為賽事已經打完。
  因此徽章旁固定加註「美東」標籤與 `台灣 M/D 開打`（`taiwan_play_dates()`）。
  該函式是**依每場實際開賽時間**逐場換算，不是單純美東日期 +1——少數上午場（如 11:10 AM ET）
  在台灣仍是同一天，此時會顯示 `8/1~8/2` 範圍。
- 標題列的 `PRO v{APP_VERSION}` 取自模組頂端的 `APP_VERSION` 常數（不再硬寫在模板裡）。
  **使用者看得到，有新增或改變功能時記得往上調**；歷史上曾一路改功能卻忘了進版。

## 網路抓取的韌性（2026-08-05 調整）

- `fetch_url` 退避改為 `backoff_factor ** (attempt + 1)`＝ **3s / 9s**。
  舊版是 `1.5 ** attempt`，第一次退避是 `1.5**0 = 1 秒`（指數從 0 起算，等於浪費一次機會），
  三次總共只等 2.5 秒。**關鍵在「失敗得很快」的情境**：逾時的話三次橫跨約 47 秒還合理，
  但 covers 直接回 429/503 時，三次會在 2.5 秒內打完——對方限流 30 秒、我們 2.5 秒就放棄。
  一次執行要打 16 次請求（列表 1 + 單場 15）、間隔只 1 秒，本來就容易踩限流。
  另外會優先聽 `Retry-After` 標頭（上限 60 秒，垃圾值則退回預設退避）。
- **抓取失敗以前是完全靜默的**：某場失敗 → `parse_matchup_details` 回 None → 印「[跳過]」→
  該場**整場從當日資料消失**，網頁上毫無跡象。現在 `main()` 會收集 `failed_matchups`，
  透過 `generate_html_dashboard(failed_count=, expected_count=)` 在頁首輸出黃色警告列
  （`.fetch-warning`），明說「有 N 場沒抓到，不是當天沒有這場比賽」。沒失敗時完全不輸出。
  這與「趨勢未發佈」是**不同**的狀況，兩者文案要分開，別混用。

## 前端外部資源

- Google Fonts 改為 `media="print" onload="this.media='all'"` 非阻塞載入（另有 `<noscript>` 備援）。
  原本是一般 stylesheet，會**阻塞首次繪製**，而 Noto Sans TC 是中文字型、檔案不小——
  使用者在台灣用手機看，這是最可能拖慢首屏的一項。`font-family` 尾端有 `sans-serif` 墊底。
- **球隊 logo 刻意「不」內嵌**（30 個 SVG，來自 img.covers.com）。實測數據：
  原始 SVG 合計 128 KB，base64 內嵌後頁面 188→359 KB（gzip 31→91 KB），
  URL-encode 內嵌 188→351 KB（**gzip 31→79 KB，增加 49 KB**）。
  logo 是 `<img>`、**不阻塞首次繪製**且有 `onerror` 隱藏；內嵌等於把 49 KB 搬到
  **會阻塞的 HTML 主文件**上，首屏反而變慢。要重新評估時記得：換到的是離線韌性，不是速度。

## 技術地雷

- **降級的預設值不可以「猜一個方向」**（2026-08-28）。`determine_side_term` 抓不到盤口時
  原本回「讓分」，等於憑空指定了一個**方向**——而讓分與受讓是相反的兩邊，猜錯就是叫
  使用者去下反邊。這比顯示「未知」嚴重得多，而且完全靜默。
  掃 93 天 / 292 個版本：562 筆讓分推薦裡有 23 筆（4.1%）是這樣猜出來的。
  現在抓不到回 `None`，`analyze_betting_recommendations` 在讓分方未知時
  **整個 Run Line 市場跳過**（獨贏不受影響，它不需要盤口方向）。
  **少一筆推薦，好過給一筆反邊的。**
  新增任何降級行為時先問：這個預設值是「我不知道」還是「我猜一個」？
  同一族的還有 `MIN_GAME_TOTAL`（見上面的未結案條目）與 `fmt_hit` 的先驗。

- **CSS 與 JS 已抽出 f-string（2026-08-06），大括號照正常寫,不要再加倍。**
  改到 `DASHBOARD_CSS` / `DASHBOARD_JS` 這兩個模組層級常數時，`{` 就寫 `{`。
  它們刻意**不是** f-string。以前整段長在 `generate_html_dashboard` 的巨大 f-string 裡，
  876 個加倍括號只為了服務 15 個插值，寫錯還不會報錯（歷史上出過兩次 JS 語法錯誤）。
  抽出後模板從 2586 行縮到 160 行、加倍括號歸零。
  - JS 唯一需要 Python 值的地方是門檻常數，用 `__TOP_PICK_MIN_SCORE__` 哨符在組頁面時替換，
    維持「門檻只有一個來源」。要再加設定值請比照，**不要把 JS 改回 f-string**。
  - 只有剩下的 HTML 骨架仍是 f-string（14 個插值），那裡才需要注意大括號。
- **摺疊區 `.accordion-content` 的展開高度不可寫死**（2026-08-05 修）。
  它是 `max-height` + `overflow: hidden` 做動畫，原本 `.expanded` 寫死 `max-height: 1500px`。
  桌機雙欄時內容才 ~720px 沒事，**窄螢幕 `.accordion-inner` 與 `.rf-list` 都塌成單欄**，
  兩欄趨勢加上 8 條近期走勢實測要 1650~1965px，15 張卡**全部**被切掉最多 465px。
  症狀很容易誤判：近期走勢「只看得到第一、二條」，但長按選取文字往下拖又能把內容拖出來
  （那是 overflow 容器被選取行為捲動）。
  現在由 `syncAccordionHeight()` 在 `toggleExpand()`／`scrollToMatch()`／`resize` 時
  以 `scrollHeight` 寫成 inline style，CSS 那個 4000px 只是無 JS 時的保底。
  **在摺疊區裡加任何新區塊都不必再調數字，但別把它改回固定值。**
- `.rf-block` 是 `.accordion-inner` 的**兄弟節點**（不在裡面），拿不到那 24px padding，
  所以自己帶 `padding: 18px 24px 24px`。分隔線要整寬，故用 padding 不用 margin。
  在摺疊區加同層區塊時記得比照，否則文字會貼齊卡片邊框。
- **在 Top 5 清單那類卡片裡塞新標籤前，先確認窄螢幕不會橫向溢出**。
  `.top-col` 是 grid 項目、`.top-rec-item-info` 是 flex 項目，兩者 `min-width` 預設 `auto`(=min-content)，
  只要內部有不換行的內容（例如 `white-space: nowrap` 的走勢藥丸）就會把整欄撐爆、整頁可橫向捲動。
  兩處都已加 `min-width: 0`，`.top-rec-item-bet` 也在 ≤768px 改為可換行（否則藥丸會被 ellipsis 切掉）。
  2026-08-02 加走勢藥丸時就踩過一次，當時只測桌機沒發現。
  驗證方法：把頁面塞進 390px 寬的 iframe，檢查 `scrollWidth - clientWidth` 是否為 0
  （直接縮視窗沒用，Chrome 視窗有最小寬度限制）。
- 只輸出 `index.html`（GitHub Pages 直接用）；`mlb_trends.html` 已於 2026-07-10 移除。
- **隊名中英切換鈕已於 2026-08-21 移除**（使用者從來沒用過）。全站固定繁中，
  `currentLanguage` / `toggleLanguage()` 一併刪掉。
  ⚠️ 但 `translateText()` 與隊名字典**必須留著**——畫面上的隊名、推薦文案、
  Recent Form 的 `@@佔位符@@`、詳細趨勢的隊名全都靠它翻，不是只服務那顆按鈕。
  （更早以前還踩過「記憶 'en' 導致預設變英文」，所以本來就不記 localStorage。）
- **開賽時間顯示「主場當地時間 ｜ 台灣時間」，不再顯示 ET**（2026-08-21 改）。
  由 `game_time_variants()` 在 Python 算好存進 JSON 的 `local_time` / `taiwan_time`，
  前端 `formatGameTime()` 只負責顯示——**時區換算集中在 Python 一處，不要散到 JS**。
  - 主場當地時間用 `HOME_TZ_OFFSET`（相對 ET 的時差，亞利桑那不實施夏令時間已含在表內），
    和 `is_day_game` 的判定基準一致；以前畫面只寫 ET，和「下午場」的說明對不起來。
  - 台灣時間用 24 小時制**且一定帶日期**：美東晚場對應台灣隔天凌晨，
    不寫日期會被誤讀成當天；半夜的 `00:40` 也比 `12:40 AM` 好判讀。
  - 舊的 index.html 沒有這兩個欄位，`formatGameTime()` 會退回顯示 `game_time`（ET），
    所以拿舊檔案跑 `--replay` 不會壞。
- 零外部依賴（純標準庫）是刻意的設計，新增功能請維持。

## 驗證方式

改完**爬蟲**後：`python scrape.py` 跑一次，檢查產出 index.html 內嵌 JSON
（`top-ai-data` / `top-sides-data` / `top-totals-data`）數量與欄位，
並確認 `game_time` 不是全 "None"。趨勢稀疏時段（美東凌晨）推薦為 0 是正常的。

改**版面／樣式**時用 `python scrape.py --replay`：不連網，直接讀回 index.html 內嵌的
四份 JSON 重新產生頁面，**0.3 秒**（完整爬蟲要 16 次請求、約一分鐘，還會多打擾 covers 一次）。

`--replay` 也是重構樣板時的安全網：輸入輸出是同一份資料，所以**只要行為沒變，
產出就該位元組完全相同**。做法是先 `cp index.html baseline.html`，改完跑 `--replay`，
再 `cmp index.html baseline.html`。CSS/JS 抽出 f-string 那次就是這樣驗證的
（2586 行搬家、876 個括號改寫，cmp 一次證明零行為差異）。

UI 改動另外一定要用 390px iframe 量 `scrollWidth - clientWidth`（見技術地雷）。

### 不變式掃描：找「靜默失敗」型 bug 的方法（2026-08-26 建立）

這個專案最危險的 bug 都是同一族：**解析不到就退回預設值，畫面照常顯示、不報錯**，
只有比對原始資料才看得出數字是錯的。逐行讀 4838 行程式沒有效率，
**對歷史產出跑不變式檢查**才是有效的做法——哪裡違反，哪裡就是 bug。

跑過一輪的檢查項（91 天、1198 場、9306 句趨勢、1612 筆推薦、1218 筆 Top 5）：

| 不變式 | 說明 |
|---|---|
| **分數可從畫面紀錄重算** | 由 `hit_detail` 的 N/M 重算 Wilson 下界，應等於 `score`。**最有力的一項**——任何「計分用的值與顯示的值不同」都會被抓到 |
| **戰績可從 history.json 重算** | `compute_track_record()` 的結果應出現在網頁上 |
| `wins <= sample`、樣本可解析 | 抓解析漏洞 |
| 讓分只有 ±1.5、全場總分 **6.5**~15.0 | 抓盤口解析錯誤（⚠️ 別抄 `MIN_GAME_TOTAL`，見下）|
| `game_time` 不是 None、時區換算可重算 | 抓時間欄位問題 |
| F5／Team Total 不得混入、Units/ROI 不得殘留 | 抓過濾漏網 |
| Top 5 無 <50 分、無同場同隊重複、無互斥組合 | 抓排序規則失效 |
| `gid` 都能對到 games、中譯無英文殘留 | 抓資料完整性 |

⚠️ **解析預警只印在 Actions 日誌，不會出現在網頁上**。趨勢解析、比分、全場總分
那幾道 `[!] 警告` 都是 `print()`，給除錯用的。唯一會顯示給使用者的是頁首的
`.fetch-warning`（有 N 場沒抓到）——兩者用途不同，加新的預警前先想清楚是哪一種。

⚠️ **關鍵在看「最後出現日期」而不是總筆數**。多數違反都是修正前的舊資料，
總筆數動輒上千很嚇人；但只要最後出現日期停在對應的修正日，就代表現行程式已經不會產生。
真正要追的是**最後出現日期落在最近幾天**的那些。

⚠️ 判讀殘留英文時要先排除**刻意保留**的：主審／投手人名、`WHIP` 這類術語、
`RF_CITIES` 只收城市不收隊名、以及 covers 自己漏了隊名的句子。
第一版沒排除，1101 筆「殘留」裡有 857 筆是人名，差點被當成 bug。

### ⚠️ 未結案：全場總分曾出現 5.5（2026-08-26，根因未知）

使用者回報「釀酒 大都會 小分是 5.5?」。數字幾乎確定是錯的：

- 當天其他 7 場都是 7.5~10.5
- 掃 91 天 / 472 筆大小分推薦，**5.5 只出現過那一次**；
  分佈是 6.5~14.5，主群 7.5(113) / 8.5(124)，**沒有一筆低於 6.5**
- 5.5 是首五局 (F5) 的典型數字，與 2026-07-27 的「小 4.5」同一族

**根因還沒查到**——隔天同一組對戰是 7.0（正常），那場的頁面已經過去，
無從重現。已在 `diagnose_total_line()` 架好預警：選出的總分低於
`SUSPICIOUS_GAME_TOTAL`(6.5) 時，印出摘要表的標籤順序、`Total` 區段的
所有候選值，以及 `Game Line - Total - FT` 區段的報價。正常值完全靜默。

**下次看到日誌有這個警告，就是查根因的機會，別錯過。**

⚠️ **刻意沒把 `MIN_GAME_TOTAL` 從 5.5 提到 6.5**（2026-08-27 與使用者確認過）。
還沒查到根因就調門檻是治症狀：萬一真正的原因是解析取錯欄位（例如摘要表結構變了），
調門檻只會讓下一次以不同形式出現。要改門檻請當成一個明確的取捨來做，不是順手調參數。

⚠️ 這次稽核沒抓到它，是因為不變式寫成「5.5 <= 總分 <= 15.0」——**直接抄了程式裡的
`MIN_GAME_TOTAL`**。拿程式自己的假設去檢查程式是循環論證，5.5 剛好卡在下限通過。
**不變式的參考值要來自實際資料的分佈，不是來自程式碼裡的常數。**

### ⚠️ 做歷史掃描前先確認 clone 不是 shallow（2026-08-26 踩到）

雲端 session 的 clone **預設是 shallow**，`git log -- index.html` 只看得到最近幾十個 commit。
那次掃描回報「全部歷史 1016 句」，實際上只涵蓋 8/13 之後 **13 天**；
`git fetch --unshallow` 之後才是 275 個版本、90 天、**7348 句**——差了 7 倍。

危險的是**它不會報錯**，掃描照跑、數字照出，只是樣本悄悄小了一個數量級，
結論看起來一樣可信。開始掃描前先確認：

    [ -f .git/shallow ] && git fetch --unshallow
    git rev-list --count origin/main -- index.html   # 應該是幾百，不是幾十

同理，任何「掃全部歷史」的結論都要先講清楚實際涵蓋幾天幾筆，不要只寫「全部歷史」。
